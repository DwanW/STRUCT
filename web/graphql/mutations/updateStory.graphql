mutation UpdateStory(
  $id: Int!
  $title: String!
  $overview: String!
  $tags: String!
) {
  updateStory(id: $id, title: $title, overview: $overview, tags: $tags) {
    id
    title
    overview
    tags
    updatedAt
    createdAt
  }
}
