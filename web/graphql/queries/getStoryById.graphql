query getStoryById($id: Int!) {
  getStoryById(id: $id) {
    id
    title
    overview
    up_vote
    down_vote
    cover_url
    status
    tags
    createdAt
    creator {
      id
      username
      avatar_url
      endorsed
      about
    }
  }
}
