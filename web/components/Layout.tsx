import React from 'react'

interface LayoutProps {

}

const Layout: React.FC<LayoutProps> = ({}) => {
        return (
            <div>
                Layout
            </div>
        );
}

export default Layout